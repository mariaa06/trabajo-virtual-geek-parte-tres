const job = "developer"; 

 const person = {
    firstName: 'Maria',
    lastName:'Velásquez',
    age: 18,
    job: job, }

document.write(`Mi nombre es ${person.firstName} ${person.lastName}, 
tengo ${person.age} años y mi profesión es ${person.job}`);